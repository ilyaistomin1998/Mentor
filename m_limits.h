#ifndef MLIMITSH
#define MLIMITSH
#include "m_expressions.h"

char *lim111t (const range& r, char *text);
char *lim112t (const range& r, char *text);
char *lim113t (const range& r, char *text);
char *lim114t (const range& r, char *text);
char *lim115t (const range& r, char *text);
char *lim121t (const range& r, char *text);
char *lim122t (const range& r, char *text);
char *lim123t (const range& r, char *text);
char *lim124t (const range& r, char *text);
char *lim125t (const range& r, char *text);
char *lim126t (const range& r, char *text);
char *lim131t (const range& r, char *text);
char *lim132t (const range& r, char *text);
char *lim141t (const range& r, char *text);
char *lim151t (const range& r, char *text);
char *lim161t (const range& r, char *text);
char *lim211t (const range& r, char *text);
char *lim212t (const range& r, char *text);
char *lim221t (const range& r, char *text);
char *lim231t (const range& r, char *text);
char *lim241t (const range& r, char *text);
char *lim242t (const range& r, char *text);
char *lim243t (const range& r, char *text);
char *lim244t (const range& r, char *text);
char *lim245t (const range& r, char *text);
char *lim251t (const range& r, char *text);
char *lim252t (const range& r, char *text);
char *lim261t (const range& r, char *text);
char *lim262t (const range& r, char *text);
char *lim271t (const range& r, char *text);
char *lim281t (const range& ar, char *text);
char *lim291t (const range& r, char *text);
char *lim311t (const range& r, char *text);
char *lim312t (const range& r, char *text);
char *lim313t (const range& r, char *text);
char *lim314t (const range& r, char *text);
char *lim315t (const range& r, char *text);
char *lim316t (const range& r, char *text);
char *lim317t (const range& r, char *text);
char *lim318t (const range& r, char *text);
char *lim319t (const range& r, char *text);
char *lim3110t (const range& r, char *text);
char *lim321t (const range& r, char *text);
char *lim322t (const range& r, char *text);
char *lim323t (const range& r, char *text);
char *lim324t (const range& r, char *text);
char *lim325t (const range& r, char *text);
char *lim326t (const range& r, char *text);
char *lim331t (const range& r, char *text);
char *lim332t (const range& r, char *text);
char *lim333t (const range& r, char *text);
char *lim334t (const range& r, char *text);
char *lim335t (const range& r, char *text);
char *lim336t (const range& r, char *text);
char *lim337t (const range& r, char *text);
char *lim338t (const range& r, char *text);
char *lim341t (const range& r, char *text);
char *lim342t (const range& r, char *text);
char *lim343t (const range& r, char *text);
char *lim344t (const range& r, char *text);
char *lim345t (const range& r, char *text);
char *lim346t (const range& r, char *text);
char *lim347t (const range& r, char *text);
char *lim348t (const range& r, char *text);
char *lim349t (const range& r, char *text);
char *lim3410t (const range& r, char *text);
char *lim3411t (const range& r, char *text);
char *lim3412t (const range& r, char *text);
char *lim3413t (const range& r, char *text);
char *lim411t (const range& r, char *text);
char *lim412t (const range& r, char *text);
char *lim413t (const range& r, char *text);
char *lim414t (const range& r, char *text);
char *lim421t (const range& r, char *text);
char *lim422t (const range& r, char *text);
char *lim423t (const range& r, char *text);
char *lim424t (const range& r, char *text);
char *lim431t (const range& r, char *text);
char *lim432t (const range& r, char *text);
char *lim433t (const range& r, char *text);
char *lim441t (const range& r, char *text);
char *lim442t (const range& r, char *text);
char *lim451t (const range& r, char *text);
char *lim461t (const range& r, char *text);
char *lim462t (const range& r, char *text);
char *lim471t (const range& r, char *text);
char *lim481t (const range& r, char *text);
char *lim482t (const range& r, char *text);
char *lim483t (const range& r, char *text);
char *lim491t (const range& r, char *text);
char *lim492t (const range& r, char *text);
char *lim511t (const range& r, char *text);
char *lim512t (const range& r, char *text);
char *lim513t (const range& r, char *text);
char *lim514t (const range& r, char *text);
char *lim515t (const range& r, char *text);
char *lim516t (const range& r, char *text);
char *lim521t (const range& r, char *text);
char *lim522t (const range& r, char *text);
char *lim523t (const range& r, char *text);
char *lim524t (const range& r, char *text);
char *lim525t (const range& r, char *text);
char *lim531t (const range& r, char *text);
char *lim532t (const range& r, char *text);
char *lim533t (const range& r, char *text);
char *lim541t (const range& r, char *text);
char *lim542t (const range& r, char *text);
char *lim543t (const range& r, char *text);
char *lim544t (const range& r, char *text);
char *lim545t (const range& r, char *text);
char *lim546t (const range& r, char *text);
char *lim547t (const range& r, char *text);
void split_lim (const char *all, char *lim, char *sol);

#endif
